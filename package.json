{
  "name": "spotify-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "server": "node index",
    "client": "cd client && npm start",
    // "postinstall": "cd client && npm install",
    "postbuild": "cd client/ && npm install --production && npm build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.25.0",
    "concurrently": "^7.0.0",
    "dotenv": "^14.3.0",
    "express": "^4.17.2",
    "react-router-dom": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
